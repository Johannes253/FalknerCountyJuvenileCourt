@page
@model FalknerCountyJuvenileCourt.Pages.FilingDecisions.IndexModel

@{
    ViewData["Title"] = "Filing Decisions";
}
<head>
    <style>
        #bottom {
      position: absolute;
      bottom: 0;
      width: 100%;
      background: white;
      text-align: center;
      padding: 10px 0;
    }
    </style>
</head>
<div id=bottom>
    Here you are able to view the details of a type of filing decision.
</div>

<h1>Filing Decisions</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FilingDecisions)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.FilingDecisions) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
