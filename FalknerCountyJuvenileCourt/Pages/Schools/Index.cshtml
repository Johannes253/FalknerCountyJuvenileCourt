@page
@model FalknerCountyJuvenileCourt.Pages.Schools.IndexModel

@{
    ViewData["Title"] = "Schools";
}


<h1>Schools</h1>

<p>
    <a id="Cbtn" asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Schools)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Schools) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a id="EDbtn" asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a id="EDbtn" asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
